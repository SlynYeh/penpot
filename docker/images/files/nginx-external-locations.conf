# External upstreams disabled: serve 404 instead of proxying to GitHub / Google Fonts.
location ~ ^/github/penpot-files/(.+)$ {
    return 404;
}

location ~ ^/internal/gfonts/font/(?<font_file>.+) {
    return 404;
}

location ~ ^/internal/gfonts/css {
    return 404;
}
